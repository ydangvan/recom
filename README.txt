- Download the fodler

- Instal Flask. Again,  Flask framework to create a web page with a form to input a movie title and a button to submit the form. When the form is submitted, the application uses the OMDB API to retrieve information about the movie, including its title, year, and genre. The retrieved information is then displayed on the web page.

- Make sure to run the app.py by typing in the terminal: python app.py

- Use this to test http://127.0.0.1:5000